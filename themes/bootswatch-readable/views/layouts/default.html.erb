<!DOCTYPE html>
<html lang="en">

<!--<%#= This little regex is all about stripping out the | character if theres nothing after it!  %>//-->
<% if /(\w+('\w+)?)(\s\w+('\w+)?)*\s+\|\s+$/.match(page_title) %>
    <% page_title.gsub!(/\s+\|\s+/, '') %>
<% end %>

<%= render 'shared/blog_head' %>

<body>
    <div class="bg-blog-1"></div>
    <div id='page' class='container'>
        <%= render 'shared/nav' %>
        <div class="articlewrapper">
            <div class="page-top blog-top"></div>
            <h3 class="header header-blog"><span>a word</span> from the pastor</h3><%#= controller.controller_name %>
            <div class='col-sm-8 main-column'>
                <%= yield %>
            </div>
            <div class='col-sm-4 blog-sidebar' id='sidebar'>
                <%= render_sidebars %>
            </div>
        </div>
    </div>

    <!--<div id='page' class='container'>
        <div class='row'>
            <div class='span1'></div>
            <div class='span7'></div>
        </div>
    </div>-->
    <%= render 'shared/footer' %>
</body>
</html>
